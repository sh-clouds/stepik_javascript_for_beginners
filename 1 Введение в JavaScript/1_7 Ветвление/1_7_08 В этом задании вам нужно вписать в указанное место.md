В этом задании вам нужно вписать в указанное место код, который будет присваивать переменной "х" название цифры, переданной в переменную "а". Цифра в переменную "а" будет передана случайным образом и может оказаться в диапазоне от 0 до 9 включительно, название должно быть написано по-русски с большой буквы - "Ноль", "Один", "Два" и т.д.

Sample Input:
8

Sample Output:
Восемь

Напишите программу. Тестируется через stdin → stdout
Верно решили 37 799 учащихся
Из всех попыток 30% верных


function testCase(a) {
    var x;
    // Тут нужно написать решение
    switch (a) {
      case 0:                               // если a равна единице то записываем 
        x = "Ноль";  // в переменную x фразу "Переменная a равна единице!"
        break;            
      case 1:                               // если a равна единице то записываем 
        x = "Один";  // в переменную x фразу "Переменная a равна единице!"
        break;
      case 2:
        x = "Два";
        break;
      case 3:
        x = "Три";
        break;
      case 4:                               // если a равна единице то записываем 
        x = "Четыре";  // в переменную x фразу "Переменная a равна единице!"
        break;            
      case 5:                               // если a равна единице то записываем 
        x = "Пять";  // в переменную x фразу "Переменная a равна единице!"
        break;
      case 6:
        x = "Шесть";
        break;
      case 7:
        x = "Семь";
        break;            
      case 8:
        x = "Восемь";
        break;            
      case 9:
        x = "Девять";
        break;                        
      default:
        x = "Ноль";
	}
    return x;
}
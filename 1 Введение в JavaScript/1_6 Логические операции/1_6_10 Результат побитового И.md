В этой задаче вам нужно вернуть результат побитового И переменной a с переменной b.

Пример ввода и вывода, показанные ниже, являются примером. Они показывают, что для значений переменных «a» и «b», как в примере ввода, например 1 и 10, ваш результат должен соответствовать указанному в примере вывода. Например, если входные данные 1 и 10, то результат должен быть равен 0.

Sample Input:
2 1

Sample Output:
0

Напишите программу. Тестируется через stdin → stdout
Верно решили 719 учащихся
Из всех попыток 75% верных


function testLogical(a, b) {
    let x = a & b;
    return x;
}

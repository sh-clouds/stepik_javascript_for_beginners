В этой задаче вам необходимо вернуть результат конъюнкции (логического И) переменной a, сдвинутой влево на b бит, с переменной b, сдвинутой вправо на a бит.

Sample Input:
4 7

Sample Output:
0

Напишите программу. Тестируется через stdin → stdout
Верно решили 668 учащихся
Из всех попыток 26% верных

function testLogical(a, b) {
    let x = (a<<b) && (b>>a)
    return x;
}
В этом тесте вам нужно сопоставить объявления переменных и их тип.

Сопоставьте значения из двух списков

Верно решили 896 учащихся
Из всех попыток 69% верных


let x;				переменная х имеет тип undefined
let x = 5;			переменная х имеет тип number
let y = '5';		переменная y имеет тип string
let z = "5";		переменная z имеет тип string
let x = true;		переменная х имеет тип boolean
let x = 'true';		переменная х имеет тип string
let y = 0;			переменная y имеет тип number
let y = false;		переменная y имеет тип boolean
let z = 5;			переменная z имеет тип number








